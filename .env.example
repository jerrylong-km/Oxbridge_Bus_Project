# 环境变量模板 —— 可提交到代码库，供其他机器参考
# 使用方法：复制本文件为 .env，再填入真实的值
#   cp .env.example .env
#
# 生成一个强随机 SECRET_KEY：
#   python3 -c "import secrets; print(secrets.token_urlsafe(48))"

# JWT 令牌签名密钥（请替换为你自己生成的随机值，切勿使用此处占位符）
SECRET_KEY=请在此填入你生成的随机密钥
